name: Apply DB seeds on main

on:
  push:
    branches: [ main ]
    paths:
      - 'lib/db/seeds/**'

jobs:
  apply-seed:
    name: Apply SQL seed
    runs-on: ubuntu-latest
    # Require DATABASE_URL secret to be present
    if: ${{ secrets.DATABASE_URL != '' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install psql client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      - name: Diagnostics: psql and DB connectivity
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          echo "--- Diagnostics start ---"
          echo "psql binary: $(which psql || echo 'not found')"
          psql --version || true
          echo "Checking lib/db/seeds directory:"
          ls -la lib/db/seeds || true
          echo "Attempting simple DB query (SELECT 1). This step will fail if connectivity or credentials are incorrect."
          psql "$DATABASE_URL" -c "SELECT 1;" || echo "DB connectivity test failed"
          echo "--- Diagnostics end ---"

      - name: Run seed SQL
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          echo "Applying seed: lib/db/seeds/*.sql"
          for f in lib/db/seeds/*.sql; do
            echo "--- Running $f ---"
            psql "$DATABASE_URL" -f "$f"
          done

  notify-missing-secret:
    name: Notify missing DATABASE_URL secret
    runs-on: ubuntu-latest
    if: ${{ secrets.DATABASE_URL == '' }}
    steps:
      - name: Abort and inform
        run: |
          echo "DATABASE_URL secret is not set. Seed was NOT applied."
          echo "Please add the DATABASE_URL secret in repository settings to enable automatic seed execution."
          exit 1
